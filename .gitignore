# =========================
# 构建产物
# =========================
# Maven 编译、测试、打包都会生成 target 目录，这些文件可以随时重新生成，不应该提交。
target/
# 如果后续引入 Gradle，也排除 Gradle 的构建输出。
build/
# 部分 IDE 或手动 javac 可能生成 out 目录，也不提交。
out/

# =========================
# IDE / 编辑器本地配置
# =========================
# IntelliJ IDEA 工程配置，通常包含本机路径和个人偏好，不适合提交。
.idea/
*.iml
# VS Code 工作区配置，后续如需共享推荐插件，可以单独提交经过整理的 .vscode 配置。
.vscode/
# Eclipse / Spring Tool Suite 本地工程文件。
.classpath
.project
.settings/

# =========================
# 日志文件
# =========================
# 本地运行产生的日志只用于排查，不进入代码仓库。
logs/
*.log

# =========================
# 本地环境变量和敏感配置
# =========================
# .env 往往包含数据库密码、JWT secret、AK/SK 等敏感信息，禁止提交。
.env
.env.*
# 允许提交 .env.example 这类不含真实密钥的示例文件。
!.env.example
# 本地专用配置和密钥配置不提交；正式项目应通过环境变量或部署平台注入。
application-local.yml
application-secret.yml

# =========================
# 本地数据库和运行时文件
# =========================
# H2 文件库、SQLite 或其他本地数据库文件不提交；当前项目第一阶段使用 H2 内存库。
*.db
*.mv.db
*.trace.db
# 本地启动脚本可能生成进程号文件，不提交。
*.pid

# =========================
# 操作系统自动生成文件
# =========================
.DS_Store
Thumbs.db

# =========================
# 测试覆盖率输出
# =========================
coverage/
*.exec

docs
spec-resource-registry.md